linters:
  # Enable all available linters.
  # Default: false
  enable-all: true
  # Run only fast linters from enabled linters set (first run won't be fast).
  # Default: false
  fast: true
  # Disable specific linters.
  disable:
    # deprecated
    - nosnakecase
    - ifshort
    - scopelint
    # disabled
    - funlen            # should enable at a later stage. 
    - depguard          # don't need this.
    - wsl               # fussy over whitespace.
    - nlreturn          # fussy over whitespace.
    # temporary disabled
    - lll
issues:
  exclude-rules:
    - path: handler/app/milestone.go
      linters:
        - maintidx
    - path: handler/app/render_model.go
      linters:
        - gocyclo
        - maintidx
    - path: internal/config/config.go
      linters:
        - gocognit
    - path: internal/pouet/pouet.go
      linters:
        - tagliatelle
    - path: internal/tags/tags.go
      linters:
        - gocyclo
        - gochecknoglobals
    - path: model/file_ansi_art.go
      linters:
        - dupl
    - path: model/file_bbs_adverts.go
      linters:
        - dupl
    - path: model/file_filepacks.go
      linters:
        - dupl
    - path: model/file_platforms.go
      linters:
        - dupl
    - path: model/file_text_files.go
      linters:
        - dupl
    - path: model/summary.go
      linters:
        - gocognit
        - maintidx
        - gocyclo
      
linters-settings:
  gomnd:
    # List of file patterns to exclude from analysis.
    # Values always ignored: `.+_test.go`
    # Default: []
    ignored-files: # magic number false positives.
      - 'milestone.go'
      - 'interview.go'
      - 'zoo.go'      
  misspell:
    # Correct spellings using locale preferences for US or UK.
    # Setting locale to US will correct the British spelling of 'colour' to 'color'.
    # Default is to use a neutral variety of English.
    locale: US
    # Default: []
    ignore-words:
      - 'Teh' # Teh Scene.
