{{- /*  
    layout.html ~ an empty layout template for htmx.
    The htmx layout template remains empty but always results a partial HTML document intended for AJAX responses.
*/ -}}
{{- define "layout" -}}{{- block "content" .}}<!-- no content was defined -->{{end}}{{end}}
